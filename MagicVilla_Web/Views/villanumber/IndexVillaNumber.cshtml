@model IEnumerable<MagicVilla_Web.Models.Dto.VillaNumberDTO>
<div class="container villa-list-container">
    <div class="row pb-3">
        <div class="col">
            <h1 class="text-light">Artwork List</h1>
        </div>
        <div class="col text-end pt-1">
            <a asp-controller="Villa" asp-action="CreateVilla" class="btn btn-outline-primary create-villa-btn">
                <i class="bi bi-plus-square-fill"></i> Create New Villa
            </a>
        </div>
    </div>

    <table class="table villa-list-table">
        <thead>
        <tr>
            <th>Villa NO.</th>
            <th>Villa Name</th>
            <th>Rate</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.VillaNo</td>
                <td>@item?.Villa?.Name</td>
                <td>@item?.Villa?.Rate</td>
                <td>
                    <a asp-controller="VillaNumber" asp-action="UpdateVillaNumber" asp-route-id="@item.VillaNo"
                       class="btn btn-success">
                        <i class="bi bi-pencil-square"></i>
                    </a>
                    <a asp-controller="VillaNumber" asp-action="DeleteVillaNumber" asp-route-id="@item.VillaNo"
                       class="btn btn-danger">
                        <i class="bi bi-trash-fill"></i>
                    </a>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>